using UnityEngine;
using System.Collections;

public class GameManager : MonoBehaviour {
	
	protected float captureCounter;

	void Start () {
		captureCounter = 0;
	}
	
	// Update is called once per frame
	void Update () {
		if (captureCounter == 1) {
		}
	}
}
